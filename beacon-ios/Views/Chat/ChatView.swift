import SwiftUI

struct ChatView: View {
    var body: some View {
        Text("Chat")
            .font(.title)
            .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}

#Preview {
    ChatView()
}
